''
## ---- Done ----
sessionInfo()
## ============================================================
## Marine Heatwave (MHW) Time Series Analysis — Portugal (OISST, 1981-2025)
## Standalone R script (no external files needed besides the CSV below)
## ============================================================
## ---- Setup ----
required_pkgs <- c(
"tidyverse", "cowplot", "anomalize", "strucchange", "MARSS",
"broom", "Kendall", "trend", "zoo", "ggh4x"
)
missing_pkgs <- setdiff(required_pkgs, rownames(installed.packages()))
if (length(missing_pkgs) > 0) {
install.packages(missing_pkgs, repos = "https://cloud.r-project.org")
}
invisible(lapply(required_pkgs, library, character.only = TRUE))
# Replacement for archived ggseas::tsdf()
tsdf <- function(timeseries, colname = "x") {
out <- as.data.frame(timeseries)
if (is.null(colnames(timeseries))) names(out) <- "y"
out[[colname]] <- as.numeric(stats::time(timeseries))
out[c(colname, setdiff(names(out), colname))]
}
theme_data <- theme_minimal(base_size = 11) +
theme(
strip.background = element_rect(fill = "grey92", color = NA),
panel.grid.minor = element_blank(),
plot.title = element_text(face = "bold")
)
theme_set(theme_data)
palette_zone <- c(NW = "#1b9e77", SW = "#d95f02", S = "#7570b3")
linear_label <- function(model) {
co <- coef(model)
s  <- summary(model)
sprintf("slope = %s, R^2 = %s, p = %s",
signif(co[2], 3), signif(s$r.squared, 3), signif(s$coefficients[2, 4], 3))
}
## ---- Load data ----
# >>> EDIT THIS if the CSV is not in the same folder <<<
data_file <- "MHW_summary_table_long_oisst_19812025_Portugal_1000m_bathymetry.csv"
stopifnot(file.exists(data_file))
setwd("~/Documents/GitHub/marineheatwaves")
## ============================================================
## Marine Heatwave (MHW) Time Series Analysis — Portugal (OISST, 1981-2025)
## Standalone R script (no external files needed besides the CSV below)
## ============================================================
## ---- Setup ----
required_pkgs <- c(
"tidyverse", "cowplot", "anomalize", "strucchange", "MARSS",
"broom", "Kendall", "trend", "zoo", "ggh4x"
)
missing_pkgs <- setdiff(required_pkgs, rownames(installed.packages()))
if (length(missing_pkgs) > 0) {
install.packages(missing_pkgs, repos = "https://cloud.r-project.org")
}
invisible(lapply(required_pkgs, library, character.only = TRUE))
# Replacement for archived ggseas::tsdf()
tsdf <- function(timeseries, colname = "x") {
out <- as.data.frame(timeseries)
if (is.null(colnames(timeseries))) names(out) <- "y"
out[[colname]] <- as.numeric(stats::time(timeseries))
out[c(colname, setdiff(names(out), colname))]
}
theme_data <- theme_minimal(base_size = 11) +
theme(
strip.background = element_rect(fill = "grey92", color = NA),
panel.grid.minor = element_blank(),
plot.title = element_text(face = "bold")
)
theme_set(theme_data)
palette_zone <- c(NW = "#1b9e77", SW = "#d95f02", S = "#7570b3")
linear_label <- function(model) {
co <- coef(model)
s  <- summary(model)
sprintf("slope = %s, R^2 = %s, p = %s",
signif(co[2], 3), signif(s$r.squared, 3), signif(s$coefficients[2, 4], 3))
}
## ---- Load data ----
# >>> EDIT THIS if the CSV is not in the same folder <<<
data_file <- "MHW_summary_table_long_oisst_19812025_Portugal_1000m_bathymetry.csv"
stopifnot(file.exists(data_file))
setwd("~/Documents/GitHub/marineheatwaves/DATA/OUTPUT")
## ============================================================
## Marine Heatwave (MHW) Time Series Analysis — Portugal (OISST, 1981-2025)
## Standalone R script (no external files needed besides the CSV below)
## ============================================================
## ---- Setup ----
required_pkgs <- c(
"tidyverse", "cowplot", "anomalize", "strucchange", "MARSS",
"broom", "Kendall", "trend", "zoo", "ggh4x"
)
missing_pkgs <- setdiff(required_pkgs, rownames(installed.packages()))
if (length(missing_pkgs) > 0) {
install.packages(missing_pkgs, repos = "https://cloud.r-project.org")
}
invisible(lapply(required_pkgs, library, character.only = TRUE))
# Replacement for archived ggseas::tsdf()
tsdf <- function(timeseries, colname = "x") {
out <- as.data.frame(timeseries)
if (is.null(colnames(timeseries))) names(out) <- "y"
out[[colname]] <- as.numeric(stats::time(timeseries))
out[c(colname, setdiff(names(out), colname))]
}
theme_data <- theme_minimal(base_size = 11) +
theme(
strip.background = element_rect(fill = "grey92", color = NA),
panel.grid.minor = element_blank(),
plot.title = element_text(face = "bold")
)
theme_set(theme_data)
palette_zone <- c(NW = "#1b9e77", SW = "#d95f02", S = "#7570b3")
linear_label <- function(model) {
co <- coef(model)
s  <- summary(model)
sprintf("slope = %s, R^2 = %s, p = %s",
signif(co[2], 3), signif(s$r.squared, 3), signif(s$coefficients[2, 4], 3))
}
## ---- Load data ----
# >>> EDIT THIS if the CSV is not in the same folder <<<
data_file <- "MHW_summary_table_long_oisst_19812025_Portugal_1000m_bathymetry.csv"
stopifnot(file.exists(data_file))
## ============================================================
## Marine Heatwave (MHW) Time Series Analysis — Portugal (OISST, 1981-2025)
## Standalone R script (no external files needed besides the CSV below)
## ============================================================
## ---- Setup ----
required_pkgs <- c(
"tidyverse", "cowplot", "anomalize", "strucchange", "MARSS",
"broom", "Kendall", "trend", "zoo", "ggh4x"
)
missing_pkgs <- setdiff(required_pkgs, rownames(installed.packages()))
if (length(missing_pkgs) > 0) {
install.packages(missing_pkgs, repos = "https://cloud.r-project.org")
}
invisible(lapply(required_pkgs, library, character.only = TRUE))
# Replacement for archived ggseas::tsdf()
tsdf <- function(timeseries, colname = "x") {
out <- as.data.frame(timeseries)
if (is.null(colnames(timeseries))) names(out) <- "y"
out[[colname]] <- as.numeric(stats::time(timeseries))
out[c(colname, setdiff(names(out), colname))]
}
theme_data <- theme_minimal(base_size = 11) +
theme(
strip.background = element_rect(fill = "grey92", color = NA),
panel.grid.minor = element_blank(),
plot.title = element_text(face = "bold")
)
theme_set(theme_data)
palette_zone <- c(NW = "#1b9e77", SW = "#d95f02", S = "#7570b3")
linear_label <- function(model) {
co <- coef(model)
s  <- summary(model)
sprintf("slope = %s, R^2 = %s, p = %s",
signif(co[2], 3), signif(s$r.squared, 3), signif(s$coefficients[2, 4], 3))
}
## ---- Load data ----
# >>> EDIT THIS if the CSV is not in the same folder <<<
data_file <- "MHW_summary_table_long_oisst_19812025_Portugal_1000m_bathymetry.csv"
stopifnot(file.exists(data_file))
## ============================================================
## Marine Heatwave (MHW) Time Series Analysis — Portugal (OISST, 1981-2025)
## Standalone R script (no external files needed besides the CSV below)
## ============================================================
## ---- Setup ----
required_pkgs <- c(
"tidyverse", "cowplot", "anomalize", "strucchange", "MARSS",
"broom", "Kendall", "trend", "zoo", "ggh4x"
)
missing_pkgs <- setdiff(required_pkgs, rownames(installed.packages()))
if (length(missing_pkgs) > 0) {
install.packages(missing_pkgs, repos = "https://cloud.r-project.org")
}
invisible(lapply(required_pkgs, library, character.only = TRUE))
# Replacement for archived ggseas::tsdf()
tsdf <- function(timeseries, colname = "x") {
out <- as.data.frame(timeseries)
if (is.null(colnames(timeseries))) names(out) <- "y"
out[[colname]] <- as.numeric(stats::time(timeseries))
out[c(colname, setdiff(names(out), colname))]
}
theme_data <- theme_minimal(base_size = 11) +
theme(
strip.background = element_rect(fill = "grey92", color = NA),
panel.grid.minor = element_blank(),
plot.title = element_text(face = "bold")
)
theme_set(theme_data)
palette_zone <- c(NW = "#1b9e77", SW = "#d95f02", S = "#7570b3")
linear_label <- function(model) {
co <- coef(model)
s  <- summary(model)
sprintf("slope = %s, R^2 = %s, p = %s",
signif(co[2], 3), signif(s$r.squared, 3), signif(s$coefficients[2, 4], 3))
}
## ---- Load data ----
# >>> EDIT THIS if the CSV is not in the same folder <<<
data_file <- "MHW_summary_table_long_oisst_19812025_Portugal_1000m_bathymetry.csv"
stopifnot(file.exists(data_file))
data_file
library(readr)
MHW_summary_table_long_oisst_19812025_Portugal_1000m_bathymetry <- read_csv("MHW_summary_table_long_oisst.19812025.Portugal_1000m_bathymetry.csv")
View(MHW_summary_table_long_oisst_19812025_Portugal_1000m_bathymetry)
## ============================================================
## Marine Heatwave (MHW) Time Series Analysis — Portugal (OISST, 1981-2025)
## Standalone R script (no external files needed besides the CSV below)
## ============================================================
## ---- Setup ----
required_pkgs <- c(
"tidyverse", "cowplot", "anomalize", "strucchange", "MARSS",
"broom", "Kendall", "trend", "zoo", "ggh4x"
)
missing_pkgs <- setdiff(required_pkgs, rownames(installed.packages()))
if (length(missing_pkgs) > 0) {
install.packages(missing_pkgs, repos = "https://cloud.r-project.org")
}
invisible(lapply(required_pkgs, library, character.only = TRUE))
# Replacement for archived ggseas::tsdf()
tsdf <- function(timeseries, colname = "x") {
out <- as.data.frame(timeseries)
if (is.null(colnames(timeseries))) names(out) <- "y"
out[[colname]] <- as.numeric(stats::time(timeseries))
out[c(colname, setdiff(names(out), colname))]
}
theme_data <- theme_minimal(base_size = 11) +
theme(
strip.background = element_rect(fill = "grey92", color = NA),
panel.grid.minor = element_blank(),
plot.title = element_text(face = "bold")
)
theme_set(theme_data)
palette_zone <- c(NW = "#1b9e77", SW = "#d95f02", S = "#7570b3")
linear_label <- function(model) {
co <- coef(model)
s  <- summary(model)
sprintf("slope = %s, R^2 = %s, p = %s",
signif(co[2], 3), signif(s$r.squared, 3), signif(s$coefficients[2, 4], 3))
}
## ---- Load data ----
# >>> EDIT THIS if the CSV is not in the same folder <<<
data_file <- "MHW_summary_table_long_oisst_19812025_Portugal_1000m_bathymetry.csv"
stopifnot(file.exists(data_file))
